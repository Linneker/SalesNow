apiVersion: v1
kind: Service
metadata:
  name: {{ template "salesnowapi.fullname" . }}
  labels:
    app: {{ template "salesnowapi.name" . }}
    chart: {{ template "salesnowapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "salesnowapi.name" . }}
    release: {{ .Release.Name }}
